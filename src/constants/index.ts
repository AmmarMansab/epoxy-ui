export const APP_NAME = 'Epoxy UI';
export const APP_DESCRIPTION = 'Modern, scalable application with MUI, i18n, and SCSS';

export const SUPPORTED_LOCALES = ['en', 'ar'] as const;
export const DEFAULT_LOCALE = 'en' as const;

export const ROUTES = {
  HOME: '/',
} as const;

